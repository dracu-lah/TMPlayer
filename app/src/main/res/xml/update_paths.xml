<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Only the downloaded update, and only for as long as the installer is reading it. -->
    <cache-path
        name="updates"
        path="updates/" />
</paths>
