<?xml version="1.0" encoding="utf-8"?>
<!--
    The two bars on the download notification's Pause button.

    Notification actions on recent Android draw the label and drop the icon, but the framework
    still requires one and older versions still show it, so it is here rather than reused from a
    Compose vector, which a Notification cannot take.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFFFF">
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M6,5h4v14h-4z" />
    <path
        android:fillColor="#FFFFFFFF"
        android:pathData="M14,5h4v14h-4z" />
</vector>
